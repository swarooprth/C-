1. Arrays are data structures that stores similar type of data at contiguous memory
locations. 

2. It provides cache friendliness(but no other data structure provides this).

3. Provides Random Access.

4. There are two types of arrays (fixed and dynamically allocated)
Dynamic arrays means it can resize automatically whenever the situation demands.
C++: Vector, JAVA : Arraylist PYTHON : List ----->These are dynamic arrays names.

5.