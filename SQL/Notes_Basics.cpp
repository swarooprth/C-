SQL has a standard division and is divided into 4 basic langugaes:
1. Data Definition Language(DDL) : These are used to create or modify objects in your 
DBMS. Objects can be a table or your database. It includes commands like SELECT ALTER 
DROP TRUNCATE CREATE RENAME